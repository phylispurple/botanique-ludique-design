Alléger les 3 boutons CTA du hero pour qu'ils soient moins visuellement chargés.

**Changements :**
- Réduire le padding de `px-9 py-4` à `px-6 py-2.5` pour des boutons plus compacts.
- Réduire la taille de texte de `text-sm` à `text-[13px]` ou `text-xs`.
- Augmenter l'espacement entre les boutons de `gap-4` à `gap-5` ou `gap-6`.
- Conserver les couleurs existantes (terracotta Ateliers, bleu Médiation, olive Agenda) et le comportement hover brutaliste.

**Fichier concerné :** `src/pages/Index.tsx`, lignes 109-127 environ (section hero CTAs).